-r requirements.txt

pytest>=8.0,<9
pytest-cov>=5.0
httpx>=0.27
ruff==0.16.3

# Benchmarking only. CatBoost is ~329 MB and lost the model comparison, so it is
# deliberately kept out of requirements.txt: the serving image never builds one,
# and src/models/estimators.py imports it lazily inside make_catboost().
catboost==1.2.10

# EDA notebook only
jupyter>=1.0
seaborn>=0.13,<1
