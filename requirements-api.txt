# Serving-only dependencies — this is what the Docker image installs, so it is
# kept deliberately narrow. scikit-learn is present because the calibration
# wrapper and preprocessing objects are sklearn instances that must unpickle;
# matplotlib / seaborn / jupyter are not needed to serve a prediction.
#
# Versions are the ones this project was actually developed and tested against
# on Python 3.12.4, not aspirational pins.

# Core numerics
numpy==1.26.4
pandas==2.2.2
scipy==1.14.0
pyarrow==22.0.0

# Model
lightgbm==4.7.0
scikit-learn==1.2.2

# Explainability (powers POST /explain)
shap==0.52.0

# Experiment tracking / registry client
mlflow==3.8.1

# API
fastapi==0.115.14
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
pydantic-settings>=2.3,<3

# Storage / cache
SQLAlchemy>=2.0,<3
psycopg2-binary>=2.9,<3
redis>=5.0,<7

# Config
PyYAML>=6.0
python-dotenv>=1.0
